<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="me.cabshare.ratingsystem.MainActivity" >

   
    
    <!-- the 'User x' text should be dynamically set according to who is in the cab 
    also the number of buttons displayed should match the number of 
    commuters in the cab
    
    prompt each user to rate everyone in the cab since the time they enter the cab
    to the time they leave (need to make a payment) 
    
    
    scratch all that and just go to a new page that lists all the commuters (going to have
    to fetch this from ) ignore ratings until you have 5 ratings; unrated initially - this
    person has not shared 5 trips as of yet / pending rating 
    store the number of ratings and the average - ie 7 ratings, and a new one
    ((7* current avg) + new) /8-->

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/button1"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="70dp"
        android:text="Rate your fellow Commuters!"
        android:textAppearance="?android:attr/textAppearanceLarge"
        tools:ignore="HardcodedText" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button1"
        android:layout_marginTop="52dp"
        android:text="Note: you must rate at least one fellow commuter in order to make your payment and complete the cabpool."
        android:textAppearance="?android:attr/textAppearanceSmall"
        tools:ignore="HardcodedText" />

    <Button
        android:id="@+id/button1"
        style="?android:attr/buttonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:onClick="goRatings"
        android:text="Continue to Ratings!"
        tools:ignore="HardcodedText" />

</RelativeLayout>